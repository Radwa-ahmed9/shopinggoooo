buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.2' // Adjust the version based on your setup
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0'
        classpath 'com.google.gms:google-services:4.3.15' // Make sure this matches the latest stable version
        classpath 'dev.flutter:flutter-gradle-plugin:1.0.0' // Flutter Gradle plugin version (adjust as necessary)
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
